on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    uses: carlthome/workflows/.github/workflows/nix.yaml@carlthome-patch-1
    secrets:
      CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_ACTION }}
